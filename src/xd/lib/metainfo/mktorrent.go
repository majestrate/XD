package metainfo

import (
	"xd/lib/version"
)


const CREATED_BY = version.Version

// create a new torrent from a file or directory
func MakeTorrent(f string) (tf *TorrentFile, err error) {

	return
}
